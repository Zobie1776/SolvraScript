[task.default]
build = "nova build"
run = "nova run"
test = "cargo test"

[task.nightly]
build = "cargo +nightly build --all"
run = "nova run --experimental"
test = "cargo +nightly test --all"

[project]
name = "SolvraIDE"
autodetect = true
