//==============================================
// File: examples_runtime_tests.md
// Author: Solvra Systems — Generated by Codex Agent
// License: Duality Public License (DPL v1.0)
// Goal: Document SolvraScript example execution tests
// Objective: Capture .svs → .svc → VM runtime validation outcomes
//==============================================

//==============================================
// Section 1.0 — Test Summary
//==============================================
/*---------------------------------------------------------------------------
Tested 36 examples on 2025-11-15T20:58:15.825825Z; 0 passed, 36 failed.
---------------------------------------------------------------------------*/

//==============================================
// Section 2.0 — Result Matrix
//==============================================
| Example | Type | Status | Notes |
|---------|------|--------|-------|
| `solvra_script/examples/advanced_samples.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/array_index_set.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/array_test.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/async_hello.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/async_task.svc` | .svc | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/async_task.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/async_timeout.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/branch_test.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/corecall_test.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/for_loop.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/function_test.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/hello_fn.svc` | .svc | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/hello_fn.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/hello_hybrid.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/hybrid_call.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/hybrid_stub_test.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/if_else.svc` | .svc | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/if_else.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/import_demo.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/import_std.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/loop_test.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/math_test.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/member_get_set.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/member_stub_test.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/method_basic.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/method_error_not_callable.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/method_with_args.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/missing_field.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/modules/math_utils.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/nested_object.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/nexus_policy_minimal.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/object_introspection.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/object_literal_basic.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/time_smoke_test.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/while_loop.svc` | .svc | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |
| `solvra_script/examples/while_loop.svs` | .svs | FAIL | error: `cargo run` could not determine which binary to run. Use the `--bin` o... |

//==============================================
// Section 3.0 — Detailed Logs
//==============================================
### solvra_script/examples/advanced_samples.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/advanced_samples.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/array_index_set.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/array_index_set.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/array_test.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/array_test.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/async_hello.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/async_hello.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/async_task.svc
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/async_task.svc`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/async_task.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/async_task.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/async_timeout.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/async_timeout.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/branch_test.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/branch_test.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/corecall_test.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/corecall_test.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/for_loop.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/for_loop.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/function_test.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/function_test.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/hello_fn.svc
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/hello_fn.svc`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/hello_fn.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/hello_fn.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/hello_hybrid.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/hello_hybrid.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/hybrid_call.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/hybrid_call.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/hybrid_stub_test.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/hybrid_stub_test.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/if_else.svc
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/if_else.svc`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/if_else.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/if_else.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/import_demo.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/import_demo.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/import_std.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/import_std.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/loop_test.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/loop_test.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/math_test.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/math_test.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/member_get_set.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/member_get_set.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/member_stub_test.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/member_stub_test.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/method_basic.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/method_basic.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/method_error_not_callable.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/method_error_not_callable.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/method_with_args.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/method_with_args.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/missing_field.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/missing_field.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/modules/math_utils.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/modules/math_utils.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/nested_object.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/nested_object.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/nexus_policy_minimal.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/nexus_policy_minimal.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/object_introspection.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/object_introspection.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/object_literal_basic.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/object_literal_basic.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/time_smoke_test.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/time_smoke_test.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/while_loop.svc
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/while_loop.svc`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```
### solvra_script/examples/while_loop.svs
- Status: FAIL (exit 101)
- Command: `cargo run -q -p solvrascript -- solvra_script/examples/while_loop.svs`
- Output:
```
error: `cargo run` could not determine which binary to run. Use the `--bin` option to specify a binary, or the `default-run` manifest key.
available binaries: solvra_compile, solvra_disasm, solvrascript
```

//==============================================
// End of file
//==============================================
