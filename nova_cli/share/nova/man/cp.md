# cp

## Synopsis
`cp [OPTIONS] SOURCE... DEST`

## Description
Copy files or directories using the system `cp` command. All arguments and flags are forwarded verbatim.

## Examples
- `cp notes.txt backup.txt` — copy `notes.txt` to `backup.txt`.
- `cp -r src/ build/` — recursively copy the `src` directory into `build`.
