# export

## Synopsis
`export NAME=VALUE ...`

## Description
Set environment variables for the current NovaCLI session. Each `NAME=VALUE` pair updates the process environment immediately.

## Examples
- `export RUST_LOG=debug` — enable verbose logging for subsequent commands.
- `export PATH=$PATH:/opt/nova/bin` — append a directory to `PATH`.
