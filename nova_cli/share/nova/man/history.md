# history

## Synopsis
`history`

## Description
Display the recorded command history with line numbers. NovaCLI keeps history in `~/.local/share/nova/history` and deduplicates consecutive entries.

## Examples
- `history` — print the stored command list.
