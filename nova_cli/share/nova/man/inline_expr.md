# =>

## Synopsis
`=> EXPRESSION`

## Description
Evaluate a single-line NovaScript expression and print its result. The interpreter state is shared with `ns {}` blocks.

## Examples
- `=> 21 + 21` — evaluate and print `42`.
