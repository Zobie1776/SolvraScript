# nova

## Synopsis
`nova <subcommand>`

## Description
Access Nova project tools that ship with NovaCLI. The initial release supports:

- `nova version` — print the CLI version string.
- `nova help` — show help for the `nova` namespace.
- `nova about` — describe the NovaCLI goal.

Future releases can extend this namespace without altering the base shell.
