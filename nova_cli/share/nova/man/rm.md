# rm

## Synopsis
`rm [OPTIONS] TARGET...`

## Description
Remove files or directories via the host `rm` program. Options are forwarded directly.

## Examples
- `rm old.log` — delete `old.log`.
- `rm -rf build/` — forcefully remove the `build` directory.
