# Configuration Guide

- Primary config: `~/.config/nova-gui/config.toml`
- Themes: `~/.config/nova-gui/themes/*.toml`
- Use `novactl` to trigger `config_reload`, `theme_set`, and layout/profile changes.
- Profiles: `full`, `lite`, `tablet` (auto-detect logic lives in the compositor).
