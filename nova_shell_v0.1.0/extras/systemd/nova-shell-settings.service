[Unit]
Description=Nova Shell Settings Panel
After=nova-compositor.service

[Service]
ExecStart=/usr/local/bin/nova_shell_settings
Restart=on-failure
Environment=RUST_LOG=info

[Install]
WantedBy=default.target
