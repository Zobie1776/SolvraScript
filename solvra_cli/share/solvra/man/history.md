# history

## Synopsis
`history`

## Description
Display the recorded command history with line numbers. SolvraCLI keeps history in `~/.local/share/solvra/history` and deduplicates consecutive entries.

## Examples
- `history` — print the stored command list.
