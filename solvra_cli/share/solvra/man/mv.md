# mv

## Synopsis
`mv [OPTIONS] SOURCE... DEST`

## Description
Move or rename files and directories using the system `mv` utility.

## Examples
- `mv draft.txt final.txt` — rename `draft.txt` to `final.txt`.
- `mv *.log archive/` — move all log files into `archive`.
