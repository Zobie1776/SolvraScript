# unalias

## Synopsis
`unalias NAME...`

## Description
Remove one or more aliases from the registry and persist the change to the configuration file.

## Examples
- `unalias ll` — remove the `ll` alias.
- `unalias foo bar` — delete aliases `foo` and `bar`.
