# Configuration Guide

- Primary config: `~/.config/solvra-gui/config.toml`
- Themes: `~/.config/solvra-gui/themes/*.toml`
- Use `solvractl` to trigger `config_reload`, `theme_set`, and layout/profile changes.
- Profiles: `full`, `lite`, `tablet` (auto-detect logic lives in the compositor).
