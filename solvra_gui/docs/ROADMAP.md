# Roadmap

1. Wire smithay backend to handle output creation, seat/pointer, and xdg-shell.
2. Implement BSP + floating window manager with configurable keybindings.
3. Integrate Glow renderer for simple window borders and optional shadows (`full` feature).
4. Flesh out launcher fuzzy search and settings panels leveraging iced widgets.
5. Enable wasm plugin pipeline behind the `plugins` feature with wasmtime.
