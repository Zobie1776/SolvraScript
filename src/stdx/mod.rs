//! Placeholder module for the SolvraScript stdx standard library artifacts.
//! This module does not expose Rust APIs but enables the cargo module tree to
//! recognize the `stdx` directory when the crate is compiled.
#![allow(dead_code)]
