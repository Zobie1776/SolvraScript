// SolvraScript standard library: input/output helpers

fn write(value) {
    prt(value);
}

//fn writeln(value) {         // Was to be an additional method for print, audit shows to many aliases brings redundancy
//    println(value);
//}

fn read(prompt) {
    return input(prompt);
}
