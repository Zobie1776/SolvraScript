//==================================================
// File: datetime_test.svs
// Goal: Validate stdx datetime helpers
//==================================================

import "stdx/math" as math;
import "stdx/core" as core;
import "stdx/string" as string;
import "stdx/path" as path;
//import "stdx/datetime" as dt;
import "stdx/random" as rnd;
import "stdx/fs" as fs;

fn run() {
    let epoch = dt.to_date(0);
    let iso = dt.format(0, "YYYY-MM-DD");
    let year_ok = epoch["year"] == 1970;
    let day_ok = epoch["day"] == 1;
    let hour_ok = epoch["hour"] == 0;
    let iso_ok = iso == "1970-01-01";
    return year_ok && day_ok && hour_ok && iso_ok;
}

run();

//==================================================
// End of file
//==================================================
